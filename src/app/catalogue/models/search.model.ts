export class Search {
    constructor(
        public organizations: string,
        public groups: string,
        public tags: string,
        public startDate : Date,
        public endDate : Date
    ) { }
}
